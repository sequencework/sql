{
  "name": "@sequencework/sql",
  "version": "1.0.1",
  "main": "sql.js",
  "types": "sql.d.ts",
  "license": "MIT",
  "description": "Javascript tag to format SQL template literals",
  "keywords": [
    "sql",
    "template-literals",
    "node-postgres",
    "pg"
  ],
  "files": [
    "sql.js",
    "sql.d.ts",
    "pg.js",
    "pg.d.ts"
  ],
  "scripts": {
    "build": "tsc -t es5 -m commonjs -d && cp -r build/. ./ && rm -rf build",
    "clean": "rm pg.js sql.js pg.d.ts sql.d.ts 2> /dev/null",
    "test": "jest",
    "prettier": "prettier --write '**/*.{ts,js,json,css,md}' --ignore-path .gitignore"
  },
  "devDependencies": {
    "@types/jest": "^23.3.2",
    "husky": "1.0.0-rc.13",
    "jest": "^23.5.0",
    "lint-staged": "7.2.2",
    "prettier": "1.14.2",
    "ts-jest": "^23.1.4",
    "typescript": "^3.0.3"
  },
  "dependencies": {},
  "jest": {
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testRegex": "(/__tests__/.*|(\\.|/)(test|spec))\\.ts$",
    "moduleFileExtensions": [
      "ts",
      "js",
      "json",
      "node"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "prettier": {
    "printWidth": 80,
    "tabWidth": 2,
    "singleQuote": true,
    "semi": false
  },
  "lint-staged": {
    "*.{ts,js,json,css,md}": [
      "prettier --write",
      "git add"
    ]
  }
}
